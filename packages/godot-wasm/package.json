{
  "name": "@browser-forge/godot-wasm",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "download": "node ./scripts/download.mjs",
    "list": "node ./scripts/list.mjs"
  },
  "description": "Godot WebGPU Wasm assets used by both the editor and the browser-side export runtime. Both bundles are served from /godot-wasm/ via the Next.js public folder."
}
